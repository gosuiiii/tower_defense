# 2026-04-14 Work Log

## Added Scoring & Victory System
- Added `score` and `fireworks_timer` fields to `Game` component
- Changed `max_level` from 10 to 5
- Scoring: +1 per enemy destroyed in `DestroyEnemy`
- Victory: after level 5, `won=true` triggers celebration
- Created `etc/assets/firework.flecs` prefab (inherits from Particle, 8 color variants)
- Added `spawn_firework()` function: spherical burst of 40-60 particles with random colors
- Added `VictoryCelebration` system: spawns fireworks every 0.3s when `won=true`

## Added 3D Pixel Font Score Display & Billboards
- Flecs sokol renderer has no 2D text rendering (EcsText unused by sokol)
- Implemented 3D pixel font using glowing Box entities (5x3 bitmap glyphs)
- `build_text_3d()`: creates colored emissive boxes to form text
- `ScoreDigit` tag component for score entities (cleanup/update tracking)
- `Billboard` tag component for billboard text entities
- `hud` scope as parent for all HUD/billboard entities
- `ScoreDisplay` system: shows "SCORE N" above map, gold color, rebuilds only on score change
- `VictoryBillboard` system: large "VICTORY!" + "SCORE N" billboard on win
- `GameOverBillboard` system: large "GAME OVER" + "SCORE N" billboard on loss
- Removed old `GameOverlay` console-only system
- Game fields added: `billboard_created`, `displayed_score`
- Test document: `test/test_score_gui.md`
